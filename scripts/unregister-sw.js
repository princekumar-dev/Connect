// Unregister service worker in development (run in browser console or via Puppeteer if needed)
// This script is for manual usage: node scripts/unregister-sw.js
// It will attempt to open a headless Chromium is not included here; provide instructions instead.
console.log('To unregister service worker in dev, open DevTools -> Application -> Service Workers -> Unregister');
